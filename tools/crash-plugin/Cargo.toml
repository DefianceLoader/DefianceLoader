[package]
name = "defiance-crash-test-plugin"
version = "0.1.0"
license = "MPL-2.0"
edition = "2021"
[workspace]
[lib]
crate-type = ["cdylib"]
[dependencies]
defiance-feature-sdk = { path = "../../crates/feature-sdk" }
[profile.release]
panic = "abort"
